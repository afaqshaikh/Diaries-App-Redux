
import AppRouter from "./Config/route";
function App() {
  // const counter = useSelector((state) => state.counter);

  return (
    <div className="App">
      <AppRouter />
    </div>
  );
}

export default App;
